"""
stockai — โครงสร้างใหม่ของระบบประเมินมูลค่าหุ้นด้วย AI
=========================================================

แพ็กเกจนี้จัดระเบียบโปรเจกต์ให้อ่านง่าย แก้ไขง่าย และต่อยอดได้:

    stockai/
      config.py        — ค่าคงที่ + path กลาง (แก้ที่เดียวใช้ทั้งระบบ)
      data_loader.py   — โหลด/ตรวจงบการเงิน JSON จากโฟลเดอร์ data/
      features.py      — สร้างชุดข้อมูลฝึก (features + label) จากงบ + ราคาจริง
      ai/trainer.py    — เทรนโมเดล (ทำนาย return 1 ปี + ตีตราถูก/แพง)
      ai/predictor.py  — โหลดโมเดลแล้วพยากรณ์รายหุ้น
      pipeline.py      — รวมขั้นตอน: โหลด → คำนวณ ratio → ประเมินมูลค่า → AI
      cli.py           — คำสั่งใช้งาน: ratios / valuation / train / predict
      api.py           — FastAPI เปิดเป็นบริการเว็บ

ส่วนคำนวณคณิตศาสตร์ (CashFlowModel, ratios, DCF) ยังคงเรียกใช้โมดูลเดิม
ใน Backend/ ที่ทำงานได้อยู่แล้ว เพื่อไม่ให้ของเดิมพัง
"""

__version__ = "2.0.0"
